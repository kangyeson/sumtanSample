<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mysumtan">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".change_infor"/>
        <activity android:name=".MainActivity_shelter"
            tools:ignore="Instantiatable"/>
        <activity android:name=".animal_search"/>
        <activity android:name=".Write_resume"/>
        <activity android:name=".Start"/>
        <activity android:name=".Login"/>
        <activity android:name=".Join"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".start_splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>